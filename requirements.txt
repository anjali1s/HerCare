# Core LangChain
langchain==0.3.7
langchain-community==0.3.5
langchain-huggingface==0.1.2
langchain-chroma==0.1.4
langchain-groq==0.2.1

# Vector store
chromadb==0.5.20

# Embeddings (local, free, no API key needed)
sentence-transformers==3.2.1

# Document loaders
pymupdf==1.24.13          # PDF parsing (PyMuPDFLoader)
docx2txt==0.8             # DOCX parsing
python-docx==1.1.2        # fallback / metadata extraction for docx

# Env management
python-dotenv==1.0.1
